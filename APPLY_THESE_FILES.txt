Replace/add these files in your project:

1. Dockerfile
2. .dockerignore
3. scripts/dev-with-save.cjs
4. scripts/save-mapdata-server.cjs
5. README_DOCKER_VOLUME.md

Then patch your src/utils/streetViewAdminStorage.js:
- Search for localhost:3010 and replace with dynamic host logic.
- Use the helper in src/utils/streetview-api-base.patch.js.

Main replacement:

const SAVE_API_BASE = `${window.location.protocol}//${window.location.hostname}:3010`;

fetch(`${SAVE_API_BASE}/upload-asset`, ...)
fetch(`${SAVE_API_BASE}/save-mapdata`, ...)

After applying, rebuild:

docker rm -f streetview-app
docker build -t streetview-app .

docker run --name streetview-app ^
  -p 5055:5055 ^
  -p 3010:3010 ^
  -v "C:\StreetViewData\uploads:/app/public/uploads" ^
  -v "C:\StreetViewData\data:/app/public/data" ^
  streetview-app
