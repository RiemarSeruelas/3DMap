node_modules/
dist/
.env
*.log

# Runtime image storage stays on the host and is never committed.
# The API recreates the panos / thumbs / maps / machines / safety-popups
# subfolders automatically when it starts.
public/uploads/**
!public/uploads/.gitkeep
